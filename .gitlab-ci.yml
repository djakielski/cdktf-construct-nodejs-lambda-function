# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

default:
  image:
    name: node:19-alpine
stages:
  - test
  - release
test:
  script:
    - yarn test
  stage: test
release:
  script:
    - yarn run release
  stage: release
  only:
    - tags
