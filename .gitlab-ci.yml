# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

default:
  image:
    name: node:19-alpine
stages:
  - test
  - release
test:
  script:
    - pnpm install
    - pnpm test
  stage: test
release:
  script:
    - pnpm install
    - pnpm run release
  stage: release
  only:
    - tags
